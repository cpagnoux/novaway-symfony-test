{% extends 'base.html.twig' %}

{% block header %}
    <nav class="navbar sticky-top navbar-expand-lg navbar-light bg-light">
        <a class="navbar-brand" href="{{ path('admin_home') }}">Back Office</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
    
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Livres
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <a class="dropdown-item" href="{{ path('book_index') }}">Livres</a>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="{{ path('author_index') }}">Auteurs</a>
                    </div>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Films
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <a class="dropdown-item" href="{{ path('dvd_index') }}">DVD</a>
                        <a class="dropdown-item" href="{{ path('bluray_index') }}">Blu-ray</a>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="{{ path('director_index') }}">Réalisateurs</a>
                        <a class="dropdown-item" href="{{ path('actor_index') }}">Acteurs</a>
                    </div>
                </li>
            </ul>
        </div>
    </nav>
{% endblock %}
