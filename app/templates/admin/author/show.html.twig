{% extends 'admin/layout.html.twig' %}

{% block title %}Auteur{% endblock %}

{% block body %}
    <h1>Auteur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>ID</th>
                <td>{{ author.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ author.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('author_index') }}">retour vers la liste</a>

    <a href="{{ path('author_edit', {'id': author.id}) }}">éditer</a>

    {{ include('admin/author/_delete_form.html.twig') }}
{% endblock %}
