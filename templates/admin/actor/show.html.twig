{% extends 'admin/layout.html.twig' %}

{% block title %}Actor{% endblock %}

{% block body %}
    <h1>Actor</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ actor.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ actor.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('actor_index') }}">retour vers la liste</a>

    <a href="{{ path('actor_edit', {'id': actor.id}) }}">éditer</a>

    {{ include('admin/actor/_delete_form.html.twig') }}
{% endblock %}
